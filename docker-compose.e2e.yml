version: '3.5'

services:
  e2e-db:
    image: postgres:11
    environment:
      - POSTGRES_PASSWORD=postgres
  e2e:
    build:
      context: ./
      dockerfile: config/test.Dockerfile
    depends_on:
      - e2e-db
